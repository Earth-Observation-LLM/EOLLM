FROZEN v2 SNAPSHOT (commit on branch paper-v2-audit-rewrite).
This is the "audit cross-view fusion / satfwd primary" draft, full-conference length (18pp).
v3 (the camera-ready editing pass: shortened, more figures, behavioral claims) lives in the
parent directory's main.tex / sections/. Build v2 from here with:
  tectonic v2_archive/main.tex   (paths inside reference ../figures etc — build from repo root or copy)
NOTE: v2_archive/main.tex \input{sections/...} resolves to the LIVE sections; to rebuild v2 exactly,
use the .tex files in v2_archive/sections/ (self-contained copies).
